import 'package:flutter/material.dart';

class HomeEventPage extends StatelessWidget {
  const HomeEventPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("HomeEvent Page"),
      ),
    );
  }
}
